# sap2-v1 leaderboard

Generated by `tools/arena_sap2.py` on 2026-09-14. Do not edit by hand.

60 seeds from `900000`, both seatings for every pair. 720 matches in 100s.

| # | bot | rating | winrate | median ms | max ms | forfeits | author | notes |
|---|---|---|---|---|---|---|---|---|
| 1 | `ppo_mlx` | 2062 | 88.9% | 0.17 | 6.0 | 0 | pjsny | PPO, 2x128 tanh, masked logits. 40 iters x 64 episodes against a pool of itself, greedy, and random. 65s on an M4 Pro GPU. |
| 2 | `greedy` | 1642 | 39.2% | 0.01 | 0.1 | 0 | pjsny | Fixed rule: combine, else buy the best-statted pet that fits, else set up a combine, else end the turn. |
| 3 | `random` | 796 | 0.0% | 0.00 | 0.1 | 0 | pjsny | Uniform over the legal mask. The rating floor. |

## Reference bots, not ranked

These break the observation-only rule on purpose, as a ceiling to measure against. See `bots/README.md` rule 2.

| bot | winrate | notes |
|---|---|---|
| `mc_search` | 71.9% | One-ply search, 4 rollouts per candidate, horizon 2 rounds, on env.clone(). Breaks the observation-only rule on purpose as a ceiling. |

## Head to head

| pair | record | draws |
|---|---|---|
| `greedy` vs `mc_search` | 12-108 | 0 |
| `greedy` vs `ppo_mlx` | 9-111 | 0 |
| `greedy` vs `random` | 120-0 | 0 |
| `mc_search` vs `ppo_mlx` | 31-89 | 0 |
| `mc_search` vs `random` | 120-0 | 0 |
| `ppo_mlx` vs `random` | 120-0 | 0 |

Ratings come from a Bradley-Terry fit on the Elo scale, field mean 1500, with one drawn game for each pair as a prior. A clean sweep has no finite maximum likelihood strength, so each rating is a conservative bound on the true separation, not a point estimate.
